<h1 align="center">Yo, I'm zaerald</h1>

```sh
$ env | egrep '^(EDITOR|LANG|SHELL|TERM_PROGRAM|USER)=' | sort -u
EDITOR=nvim
LANG=en_US.UTF-8
SHELL=/bin/zsh
TERM_PROGRAM=tmux
USER=zaerald

$ alias | grep "=$(whoami)$" | sort -u
z=zaerald
zae=zaerald
zee=zaerald

$ cat /etc/profile | sort -u
DOMAIN=zaerald.com
GITHUB=zaerald
TWITTER=zaerald
```

